# learn-go

Steve's Go projects. Each subfolder is a self-contained Go module — clone the repo, `cd` into one,
`go run .`.

| Project | What it is |
| --- | --- |
| [`survival/`](survival) | Round-based survival shooter. One ~200-line file, Ebitengine, ten waves to win. |
